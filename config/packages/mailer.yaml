framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        envelope:
            sender: 'system@paramall.tn'
        headers:
            from: 'Paramall <system@paramall.tn>'
            bcc: 'notifications@paramall.tn'
            replyTo: 'contact@paramall.tn'
            X-Custom-Header: 'Paramall'