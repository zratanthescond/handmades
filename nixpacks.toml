[phases.setup]
nixPkgs = ["php", "phpExtensions.curl", "phpExtensions.mbstring", "phpExtensions.intl", "phpExtensions.pdo_mysql", "phpExtensions.xml", "phpExtensions.zip", "phpExtensions.gd", "nodejs"]

[phases.install]
cmds = [
    "curl -sS https://getcomposer.org/installer | php -- --install-dir=/bin --filename=composer",
    "npm install -g yarn",
    "composer install --no-dev --optimize-autoloader",
    "yarn install --frozen-lockfile"
]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "php -S 0.0.0.0:3000 -t public"
