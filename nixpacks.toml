[phases.setup]
nixEnv = "./environment.nix"

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "yarn install --frozen-lockfile"
]

[phases.build]
cmds = [
    "yarn build"
]

[start]
cmd = "php -S 0.0.0.0:3000 -t public"
