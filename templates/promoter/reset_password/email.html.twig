{% extends 'email/base_email.html.twig' %}

{% block content %}

<row>
		<columns>

			<h3>Bonjour</h3>

			<p>Pour réinitialiser votre mot de passe, veuillez visiter le lien suivant</p>

			<button href="{{ url('app_reset_password', {token: resetToken.token}) }}">Changer mon mot de passe</button>

            <p>Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>


		</columns>
	</row>


{% endblock %}
