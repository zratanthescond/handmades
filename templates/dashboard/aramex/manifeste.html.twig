<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&display=swap" rel="stylesheet">


		<style type="text/css">

			body {

				font-family: 'Lato', sans-serif;
			}
		</style>

	</head>
	<body>

		<div class="container pt-5 pb-5">

			<div class="row">

				<div class="col-md-12">

					<img src="https://api.paramall.tn/img/logo.png" class="img-fluid"/>

                    <div class="d-flex flex-row mx-auto text-center">

					<h1 class="mb-5 pb-3">Manifeste</h1>

                    <span class="badge badge-info"> {{ "now" | date('Y-m-d H:i:s') }}</span>

                    </div>

                    <h4 class="fw-bold mb-3">Pickup</h4>

					<ul class="list-unstyled">

						<li>
							<strong>PickUp Id:
							</strong>
							{{ pickUp.pickUpId }}</li>

						<li>
							<strong>Guid:
							</strong>
							{{ pickUp.guid }}
						</li>


					</ul>

					<h4 class="fw-bold mb-3 mt-3">Shippements</h4>

					{% for shippement in shippements %}

						{% set order = shippement.clientOrder %}

						<ul class="list-unstyled mb-5 border-bottom pb-3">

                        <li><strong>Client: </strong>{{ order.user.fullName | capitalize }}</li>

                        <li><strong>Adresse de livraison: </strong>{{ order.user.defaultAddress | capitalize }}</li>

							<li>
								<strong>Tracking Id:
								</strong>
								{{ shippement.trackingId }}</li>

							<li>
								<strong>Nombre de produits:
								</strong>
								{{ order.products | length }}</li>

							<li>
								<strong>Services:
								</strong>
								{{ order.payementTransaction ? "NA": "CODS" }}

							</li>

							{% if not order.payementTransaction %}

								<li>
									<strong>Somme à collecter:
									</strong>
									{{ order.total | format_currency('TND', {fraction_digit: 3}) }}</li>

							{% endif %}

						</ul>

					{% endfor %}


				</div>
				<!-- ROW -->
			</div>
			<!-- container -->
		</body>
	</body>
</html>
